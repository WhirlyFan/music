# Secrets / env
.env
.env.local
*.env

# Doppler — local config + encrypted secrets fallback cache. Never commit:
# secrets come from Doppler at runtime, not the repo.
.doppler/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/

# Node / pnpm
node_modules/
.pnpm-store/
dist/
.vite/

# Django
staticfiles/
media/
*.sqlite3

# Editor / OS
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
.DS_Store
*.swp

# Docker volumes (shouldn't be committed, just in case)
postgres_data/

# Tooling caches / build artifacts (regenerated; pure churn in git)
.cache/
frontend/.tanstack/
