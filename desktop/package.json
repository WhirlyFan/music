{
  "name": "desktop",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "WhirlyFan desktop app (Tauri v2) — wraps the frontend SPA. See docs/tauri-migration.md.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
