# pnpm 11+ enforces a "minimum release age" policy by default (rejects packages
# published within the last ~7 days) as a supply-chain safeguard. For active
# development against the latest TanStack / React releases that's too strict —
# disable it project-wide. Re-enable with a value like `7d` once your stack is stable.
minimum-release-age=0
