# pnpm 11 does NOT read project settings from here — config lives in
# pnpm-workspace.yaml (minimumReleaseAge, savePrefix, …). Don't add
# save-exact / minimum-release-age here; they're silently ignored. This file is
# kept only as a signpost.
